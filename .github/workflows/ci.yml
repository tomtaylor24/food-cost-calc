name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      # 起動時に env を検証して throw するため、ビルドを通すにはダミーでも値が要る。
      # createPool は実際に接続しにいかないので、ここでは MySQL の起動は不要。
      JWT_SECRET: dummy-secret-for-ci
      DATABASE_URL: mysql://dummy:dummy@127.0.0.1:3306/dummy
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm install

      - run: npm run lint

      - run: npm run typecheck

      - run: npm run build
